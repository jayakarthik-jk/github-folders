import type { FC } from "react";

interface RightClickMenuProps {
  path: string[];
  selected: number[];
}

const RightClickMenu: FC<RightClickMenuProps> = ({ path, selected }) => {
  return <div></div>;
};

export default RightClickMenu;
